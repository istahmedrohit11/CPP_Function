#include<iostream>
using namespace std;
int sum_degits(int n){
    int sum=0;
    while(n>0){
       int digits=n%10;
       sum=sum+digits;
       n=n/10;
    }
   return sum;
}
int main(){
    int n;
    cout << "Enter degits: ";
    cin >> n;
    cout<<"sum of numbers:"<<sum_degits(n);
}
